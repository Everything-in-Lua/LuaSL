@fragment
function main(uv: vec2, time: float): vec4
    let a: vec3 = vec3(0.1, 0.4, 0.9)
    let b: vec3 = vec3(0.9, 0.2, 0.1)
    let t: float = clamp(sin(time) * 0.5 + 0.5, 0.0, 1.0)
    let c: vec3 = mix(a, b, t)
    return vec4(c.x, c.y, c.z, 1.0)
end
